仮コードを作成する問題。空欄を埋める。（①～）

メソッド：public static void main(String[] args)

　
　　変数の宣言　ユーザの推測の回数を表す数値を格納する
　　　　　　　　int型の変数　numOfGuesses　の宣言、初期値は0にする。

　①インスタンス化　SimpleDotComクラスのインスタンスを作成する。


　　演算　軍艦の位置（セル番号）を指定するため、乱数（0から4までの整数）を１つ発生させる。

　②配列の作成　int型の配列を作り、発生させた乱数に1,2を加えた値を格納。
　③メソッド呼び出し　SimpleDotComクラスのインスタンスを利用し、setLocationCells()メソッドを呼び出す。
　④変数の宣言　軍艦が沈んでいないかどうかを示すboolean型変数isAliveを宣言する。初期値はtrue。　

　　whileループ　軍艦が沈むまでの間（isAlive == true）、同じ処理を繰り返すループ
　　　値の取得　ユーザがコマンドラインで入力した値を取得
　　
　　　⑤//↓ユーザの推測した番号が正しいかどうかの確認（配列の値とコマンドラインで入力した値を比較）
　　　　メソッド呼び出し　SimpleDotComのインスタンスを利用して、checkYourself()メソッドを呼び出す。

　　　⑥//↓軍艦が沈んだかどうかの確認。
　　　　if文　checkYourself()メソッドの戻り値が"kill"かどうかの確認。
　　　　　　値の設定　isAliveにfalseを設定する（ループを抜けるという意味）
　　　　　　出力　ユーザの推測回数を画面に出力。
　　　　if文の終了

　　whileループの終了
メソッドの終了
　　

